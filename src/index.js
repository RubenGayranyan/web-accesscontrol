import React from 'react';
import ReactDOM from 'react-dom';
import LoadPage from './assets/Core';

ReactDOM.render(
    <React.StrictMode>
        <LoadPage />
    </React.StrictMode>,
    document.getElementById('page')
);